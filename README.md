# Transmute Mercury 

A React, Redux, Truffle, Material, Firebase, Ethereum, Seed dApp.

```
$ npm install
$ npm run welcome
$ truffle migrate
$ npm run start
$ npm run build:prod
$ npm run deploy:testnet
$ npm run deploy:site
```